-# content_for(:head) { javascript_include_tag 'highcharts' } #, 'highcharts/highcharts-more' }
- title t('.title')

- content_for :top do
  = render partial: 'filter', locals: {show_others_label: t('operator_report.others')}

= render partial: 'operators'