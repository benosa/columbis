- title "Claims"

%table
  %tr
    %th Num
    %th User
  - for claim in @claims
    %tr

      %td= claim.user_id
      %td= link_to 'Show', claim
      %td= link_to 'Edit', edit_claim_path(claim)
      %td= link_to 'Destroy', claim, :confirm => 'Are you sure?', :method => :delete

%p= link_to "New Claim", new_claim_path
