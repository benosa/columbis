= form_for @city do |f|
  = f.error_messages
  %p
    = f.label :country_id
    %br
    = f.select :country_id, Country.all.collect {|o| [ o.name, o.id ] }
  %p
    = f.label :name
    %br
    = f.text_field :name
  %p
    = f.submit
