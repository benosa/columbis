- content_for(:head) { javascript_include_tag 'highcharts' } #, 'highcharts/highcharts-more' }
- set_current_width(:small)

- title t('.title')

- content_for :top do
  = render partial: 'filter', locals: {show_others_label: t('direction_report.others')}

= render partial: 'directions'