- title t('.title')
= select_tag :list_filter, options_for_select(DropdownValue.available_lists.map{ |k,v| [v, k] })

= render :partial => 'table'

%p= link_to t('.new_dropdown_value'), new_dropdown_value_path
