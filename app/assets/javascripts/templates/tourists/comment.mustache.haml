%li.clearfix{id: 'tourist_comment_{{resp.id}}'}
  .managers_name
    %p
      = '{{resp.name}}'
    %p.date
      = '{{resp.date}}'
  .comment= '{{resp.body}}'
  .del_block
    %a.del.delete.delete_tourist_comment{href: '{{resp.path}}'}
