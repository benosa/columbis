- title t('.clients')
%table#clients
  %thead
    %tr
      %th= t('.full_name')
      %th= t('.passport_series')
      %th= t('.passport_number')
      %th= t('.passport_valid_until')
      %th= t('.phone_number')
      %th= t('.address')
      %th= t('.date_of_birth')
      %th
      %th
  %tbody
    =render :partial => 'client', :collection => @clients

= link_to t('.add_client'), new_client_path
