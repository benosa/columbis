- set_current_width(:small)
- content_for(:body_class) { 'panel edit_operator' }

- title t('.edit_printer')

= form_for @printer, :html => {:multipart => true} do |form|
  - @form_id = form.options[:html][:id]
  = render :partial => 'form', :locals => { :f => form }

- content_for(:float_panel) do
  = link_to t('view_all'), printers_path, :class => 'list_operator'
  = link_to t('back'), :back, :class => 'back'
  = link_to t('delete'), @printer, :class => 'delete_operator', :confirm => I18n.t('What?'), :method => :delete, id: "delete_printer#{@printer.id}"
  = link_to t('save'), '#', :class => 'save', :data => { :submit => @form_id }