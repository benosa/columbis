.current_container
  = render partial: 'charts'

  %table.totals.reservations.tour_operators
    %thead
      %tr
        %th= sort_link 'name', t('.direction')
        %th= sort_link 'amount', t('report.payment_amount'), :desc
        %th= sort_link 'items', t('report.claim_quantity')
    %tbody
      - @total.each do |row|
        %tr
          %td= row['name']
          %td= row['amount']
          %td= row['items']
