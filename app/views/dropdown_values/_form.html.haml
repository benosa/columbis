= form_for @dropdown_value do |f|
  = f.error_messages
  %table
    %tr
      %th= f.label :list
      %th= f.label :value
    %tr
      %td= select_tag :list_filter, options_for_select(DropdownValue.all(:select => 'distinct(list)').map(&:list))
      %td= f.text_field :value
  %p
    = f.submit
